// Vote endpoints are nested under /api/problems/:id/vote — see problemRoutes.js.
// This file is kept for structural completeness / future top-level vote endpoints.
const express = require('express');
const router = express.Router();

module.exports = router;
