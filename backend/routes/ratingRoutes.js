// Rating endpoints are nested under /api/solutions/:id/rate and
// /api/solutions/:id/ratings — see solutionRoutes.js. Kept for structural completeness.
const express = require('express');
const router = express.Router();

module.exports = router;
